import styled from 'styled-components'

export const AppLayoutContainer = styled.div`
  position: relative;

  overflow-x: hidden;

  .content {
    width: 100%;
    max-width: 1200px;

    padding: 0 1rem;
    margin: 0 auto;

    z-index: 20;
  }
`
